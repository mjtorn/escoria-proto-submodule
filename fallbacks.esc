:look
debug "look fallback!"
say player look_fallback:"I don't want to look at that."

:use
debug "use fallback!"
say player use_fallback:"I can't use that."

:pick_up
debug "pick_up fallback!"
say player pick_up_fallback:"I can't pick that up."

:talk
debug "talk fallback!"
say player talk_fallback:"I can't talk to that."

:open
debug "open fallback!"
say player open_fallback:"That can't be opened."

